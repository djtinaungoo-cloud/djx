name: Consolidated GitHub Pages — REMOVED

# The consolidated pages workflow has been archived to .github/workflows/pages.disabled.yml
# This placeholder prevents accidental push-triggered deployments. Use the built-in dynamic
# "pages build and deployment" workflow for push-triggered Pages deployments.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Consolidated pages workflow moved to .github/workflows/pages.disabled.yml; placeholder active."
